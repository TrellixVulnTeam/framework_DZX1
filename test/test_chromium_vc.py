import unittest
# import os
# from bci.version_control.chromium_vc import ChromiumRepo


class TestChromiumRepo(unittest.TestCase):
    """
    @classmethod
    def setUpClass(cls):
        curr_dir = os.path.dirname(os.path.realpath(__file__))
        cls.repo_path = os.path.join(curr_dir, "resources/repositories/git")
        cls.repo = ChromiumRepo(cls.repo_path)
        assert os.path.isdir(cls.repo_path)

    @classmethod
    def tearDownClass(cls):
        cls.repo_path = None
        cls.repo = None

    def test_is_repo(self):
        assert ChromiumRepo.is_repo(self.repo_path)
    """
