import unittest


class TestFirefoxRepo(unittest.TestCase):
    """
    @classmethod
    def setUpClass(cls):
        cls.commit_finder = CommitFinder("firefox", "default", 67, 68, "link-prerender", None)
        cls.repo_path = "test_repositories/mercurial"

    @classmethod
    def tearDownClass(cls):
        cls.commit_finder = None



    def test_eliminate_search_states(self):
        browser_build = FirefoxBuild(self.repo_path)
        search_states = browser_build.get_state_lineage(67, 68)
        self.commit_finder.eliminate_search_states()
    """
